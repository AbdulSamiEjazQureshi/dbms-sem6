# MongoDB Connection (includes database name in URI path)
# When using Docker with MONGO_INITDB_ROOT_USERNAME, the admin user
# is created in the 'admin' database — hence ?authSource=admin
MONGODB_URI=mongodb://admin:password@localhost:27017/dbms_webuilder?authSource=admin
