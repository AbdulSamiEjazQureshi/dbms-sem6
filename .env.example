# MongoDB Connection
MONGODB_URI=mongodb://admin:password@localhost:27017/dbms_webuilder?authSource=admin

# JWT secret for auth tokens (change in production!)
JWT_SECRET=webuilder-dev-secret-change-in-production
